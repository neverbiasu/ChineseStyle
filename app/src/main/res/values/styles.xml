<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Dark theme variant -->
    <style name="AppTheme.Dark" parent="AppTheme">
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="android:textColorPrimary">@color/text_dark</item>
        <item name="android:colorAccent">@color/accent_dark</item>
    </style>

    <!-- Light theme variant -->
    <style name="AppTheme.Light" parent="AppTheme">
        <item name="android:windowBackground">@color/background_light</item>
        <item name="android:textColorPrimary">@color/text_light</item>
        <item name="android:colorAccent">@color/accent_light</item>
    </style>

    <!-- Login theme variant -->
    <style name="AppTheme.Login" parent="AppTheme">
        <item name="android:windowBackground">@color/background_login</item>
        <item name="android:textColorPrimary">@color/text_login</item>
        <item name="android:colorAccent">@color/accent_login</item>
        <item name="colorPrimary">@color/colorPrimary_login</item>
        <item name="android:textColor">@color/text_color</item>
    </style>
</resources>

